---
title: 7signs Norway research capture — session facts
date: 2026-07-10
type: session-facts
---

- **Worked** — registered a Norway profile on https://7signs-2112.com/en/ (email/password from `research/credentials.json`, username invented as `LolaPalmer1999` since credentials.json has no username field). Captured homepage, about-us, contact-us, live-casino, games-slots, sports pages (screenshots + HTML) to `research/7signs-norway/` before the run stopped.
- **Failed** — all 3 parallel cluster subagents (cashier, promos, content) launched via `/research-capture-parallel` terminated early. Root cause: Anthropic API session limit hit mid-run (reset 7:50am Europe/Kiev), not a site or skill defect. None of the three wrote their `cluster-*.json` output before stopping.
- **Worked** — resumed the partial capture via plain `/research-capture` (non-parallel) and finished it: deposits (12 payment methods), withdrawals (12 methods, wider limits), casino_bonuses (5 permanent bonuses + public listing + account "My Bonuses" panel), cashback_offers (2), vip (1 unified page for both casino and betting), loyalty_programs (Coins→cash Shop), legal (18-section accordion T&C page + separate Privacy Notice). Wrote `manifest.json`: 13 categories — 11 `collected`, 1 `not_found` (free_spins), `vip_betting_programs` pointed at the same files as `vip_casino_programs`. All `files[]` entries verified to exist on disk via a Python check.
- **Failed** — Playwright `browser_click` (locator-based) timed out repeatedly on the account/profile sidebar panel and on several `/en/rules` accordion headings, even though the element resolved and passed Playwright's own visibility check. Root cause: the panel sits at `transform: translateX(420px)` (off-viewport) until a real dispatched click event flips its open state; a locator click either fails the "in viewport" actionability check or gets intercepted by another overlay mid-transition. Fix: call `el.click()` via `browser_evaluate` instead of `browser_click` for this component.
- **Failed** — `browser_take_screenshot` with `fullPage: true` timed out on `/en/rules` (page renders to ~51,600px tall once all 18 T&C accordion sections are expanded). Worked around with 4 scrolled viewport screenshots at increasing `scrollY` instead of one fullPage capture.
- **Verified external facts** — 7signs.com (operational domain 7signs-2112.com) casino license is No. 0000067 issued by TOBIQUE — read directly from T&C section 1.4 on `/en/rules`.
- **Decided** — treated `/en/vip` as a single unified VIP program covering both casino and sports betting (one page, one "VIP Levels" nav entry, benefit list has no sport-specific language) — per skill instructions, `vip_betting_programs` in the manifest references the same files as `vip_casino_programs` rather than being captured separately.
- **Decided** — `free_spins` marked `not_found`: no standalone permanent FS-only offer exists on the site; all free spins are bundled inside the Welcome Bonus / Weekly Reload / Weekend Reload Bonus, already filed under `casino_bonuses`.
